export type WordObject = {
    english: string;
    definition: string;
    url: string;
};

export type WordQuizObject = {
    english: string;
    definition: string;
    url: string;
    choices: string[];
};
